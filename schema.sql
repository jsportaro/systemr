CREATE TABLE person (
    id int,
    address_id int,
    name char(100),
    age int
);

CREATE TABLE place (
    id int,
    line_one char(100),
    city char(100),
    state char(100),
    zip int
);
